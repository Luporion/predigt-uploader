# Entwicklungsbericht: Phase-1-Pruefung

## Ziel

Den aktuellen Phase-1-Stand pruefend gegen `TASKS.md`, `SPEC.md`, `STATUS.md` und den Code abgleichen.

## Geänderte Dateien

- `TASKS.md`
- `STATUS.md`
- `docs/dev-log/2026-05-18_1923_phase-1-pruefung.md`

## Was wurde umgesetzt?

- Phase-1-Haekchen in `TASKS.md` an den Code-Stand angepasst.
- Offene Logdatei-Anforderung aus `SPEC.md` in `TASKS.md` sichtbar gemacht.
- `STATUS.md` beim naechsten Schritt auf Config-Fehlerfaelle und Logdatei-Klaerung aktualisiert.

## Phase-1-Abgleich

Bereits erfuellt:

- Konfiguration mit Standardwerten und optionaler `config.toml` laden.
- Terminal-Wizard mit deutscheren Texten und Pflichtfeld-Validierung.
- MP4-Pfad robust pruefen.
- Zielordner interaktiv bestimmen und bestehende Ordner bewusst bestaetigen.
- MP4 sicher kopieren/verschieben, inklusive Konfliktfall bei bestehender Zieldatei.
- FFmpeg vor MP3-Erzeugung pruefen.
- MP3-Erzeugung starten und Ergebnis auf Existenz und Dateigroesse pruefen.
- Lokalen Workflow-Erfolg nur bei gueltiger MP4 und MP3 anzeigen.
- `predigt-zusammenfassung.txt` im Zielordner schreiben.

Noch offen:

- Config-Fehlerfaelle nutzerfreundlich behandeln, z. B. ungueltige TOML-Datei oder nicht lesbare Config.
- Logdatei schreiben, wie in `SPEC.md` gefordert.
- Optional: pruefen, ob `write_summary_file` in der Config weiter gebraucht wird, da der Wizard die Textzusammenfassung inzwischen immer schreibt.

Doppelt oder unklar dokumentiert:

- `SPEC.md` fordert eine Logdatei, `TASKS.md` hatte diesen Punkt bisher nicht in Phase 1.
- `report.py` schreibt neben `predigt-zusammenfassung.txt` auch `predigt-info.json`; der Wizard kommuniziert nur die Textzusammenfassung.
- Die Config-Option `write_summary_file` wirkt unklar, weil die Zusammenfassung fuer den abgerundeten Workflow-Endzustand verpflichtend ist.

Naechste 3 sinnvolle Aufgaben:

- Config-Laden mit nutzerfreundlichen Fehlern finalisieren.
- Logdatei fuer Version 1 definieren und schreiben.
- `write_summary_file` und `predigt-info.json` bewusst entscheiden: behalten, dokumentieren oder entfernen.

## Tests

- `python -m pytest`
- Ergebnis: `36 passed in 0.15s`

## Offene Punkte / Risiken

- Es wurde nur dokumentierend geaendert, keine neue Programmlogik.

## Nächster sinnvoller Schritt

Config-Fehlerbehandlung fuer Phase 1 umsetzen und danach die Logdatei-Anforderung konkretisieren.
